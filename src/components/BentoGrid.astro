---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<div class={`grid w-full auto-rows-[20rem] grid-cols-1 md:grid-cols-3 gap-4 ${className || ""}`}>
	<slot />
</div>
